class Hero {
  // position on screen
  PVector _position;
  PVector _posOffset;
  // position on board
  int _cellX, _cellY;
  // display size
  float _size;
  
  // move data
  PVector _direction;
  boolean _moving; // is moving ? 
    
  Hero() {
  }
  
  void launchMove(PVector dir) {
  }
  
  void move(Board board) {
  }
  
  void update(Board board) {
  }
  
  void drawIt() {
  }
}
